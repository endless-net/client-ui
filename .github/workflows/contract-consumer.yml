name: Client v0 consumer

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  snapshot-unit:
    name: v0 snapshot unit (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Validate complete BA and SA trace scope
        run: |
          node --test scripts/check-client-coverage.test.mjs
          node scripts/check-client-coverage.mjs
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.38.1
          cache: false
      - uses: actions/checkout@v7
        with:
          repository: endless-net/client
          ref: ac30bfe0e959f3c93ef1059495f2356fa5476b06
          path: client-source
          persist-credentials: false
      - uses: actions/setup-go@v7
        with:
          go-version-file: client-source/clientipc/go.mod
          cache: false
      - name: Build pinned producer scenario host
        shell: bash
        working-directory: client-source/clientipc
        run: go build -o "$GITHUB_WORKSPACE/client-testserver.exe" ./cmd/client-testserver
      - name: Resolve exact consumer dependencies
        working-directory: app
        run: flutter pub get --enforce-lockfile
      - name: Test generated v0 snapshot consumer
        # The probe below checks the dependency resolved by the app itself.
        working-directory: app
        env:
          ENDLESSNET_TESTSERVER: ${{ github.workspace }}/client-testserver.exe
        run: flutter test --no-pub test/client_runtime_snapshot_test.dart test/client_event_stream_test.dart test/client_operation_test.dart test/client_mutations_test.dart test/client_state_controller_test.dart test/client_intent_journal_test.dart test/client_session_test.dart test/client_session_process_test.dart test/local_client_events_test.dart test/mobile_contract_widget_test.dart
      - name: Reject dependency drift
        run: git diff --exit-code -- app/pubspec.yaml app/pubspec.lock
      - name: Verify app transport after stream cancellation
        working-directory: app
        run: dart --packages=.dart_tool/package_config.json ../packages/local_client_rpc/tool/probe_late_headers.dart
